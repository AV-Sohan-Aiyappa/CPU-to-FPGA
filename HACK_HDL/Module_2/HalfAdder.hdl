/// HalfAdder.hdl
/// Created by Sohan on 25-03-2025
/// Computes the sum and carry of two 1-bit inputs

CHIP HalfAdder {
    IN a, b;
    OUT sum, carry;

    PARTS:
    Xor(a=a, b=b, out=sum);
    And(a=a, b=b, out=carry);
}
